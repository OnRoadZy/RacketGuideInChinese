7.9.3 合约的生存期和判定

很像上面的eq?例子，#:∃合约可以改变一个程序的行为。

具体来说，对#:∃合约的null?判断（和许多其它判断）返回#f，并改变其中一个合同的any/c意味着null?现在可能反而返回#t，导致在任意不同的行为上依赖于这个布尔值，这可能在程序持续影响。

#lang racket/exists	   package: base

解决上述问题，racket/exists库行为就像racket，但当提供#:∃合约时判断会发出错误信号。

教训：不要使用判断#:∃合约，但是如果你并不确定，用racket/exists在是安全的。
